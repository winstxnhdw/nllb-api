name: Deploy

on:
  push:
    branches: [main]
    paths: [Dockerfile]

jobs:
  deploy:
    uses: winstxnhdw/actions/.github/workflows/spaces-deploy.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    secrets:
      token: ${{ secrets.HF_TOKEN }}
