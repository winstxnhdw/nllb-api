{
	order cache before rewrite
	cache
}

:{$APP_PORT} {
	handle_path /api/* {
		handle /v1/translate {
			cache {
				allowed_http_verbs POST
				ttl 24h
			}
		}

		reverse_proxy http://localhost:{$SERVER_PORT} {
			transport http {
				versions h2c
			}
		}
	}
}
